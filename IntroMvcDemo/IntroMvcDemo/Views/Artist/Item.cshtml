@using IntroMvcDemo.DataAccess.Models
@model Artist

@{
    ViewBag.Title = Model.Name;
}

<h2>Artist Display</h2>

<ul>
    @foreach (var album in Model.Albums)
    {
        <li>@Html.ActionLink(album.Name, "Item", "Album", new { id = album.Id }, null)</li>
    }
</ul>